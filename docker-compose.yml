version: "3.9"

services:
  api:
    container_name: art_curator_api
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TIMEZONE: "Europe/Rome"
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data

